spring:
  application:
    name: price-radar-test
  main:
    banner-mode: "off"

priceradar:
  operations:
    actuator:
      username: test-actuator
      password: test-only-actuator-password
  telegram:
    enabled: false
  scheduler:
    enabled: false
